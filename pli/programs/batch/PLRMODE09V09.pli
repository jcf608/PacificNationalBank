/*********************************************************************/
/* PLRMODE09V09 — PL/I MODEL Risk Analytics V9                                    */
/* Pacific National Bank — PL/I Transaction Processing              */
/*********************************************************************/

PLRMODE09V09: PROCEDURE OPTIONS(MAIN);

  /* Standard includes */
  %INCLUDE PNBACCT;
  %INCLUDE PNBCUST;
  %INCLUDE PNBTXN;
  %INCLUDE PNBERR;

  /* Module declarations */
  DCL program_name  CHAR(12) INIT('PLRMODE09V09');
  DCL return_code   FIXED BIN(31) INIT(0);
  DCL error_msg     CHAR(80) INIT('');
  DCL records_read  FIXED BIN(31) INIT(0);
  DCL records_ok    FIXED BIN(31) INIT(0);
  DCL records_err   FIXED BIN(31) INIT(0);
  DCL eof_flag      BIT(1) INIT('0'B);
  DCL (ADDR, LENGTH, SUBSTR, INDEX, VERIFY, TRANSLATE,
       DATETIME, TRIM, HIGH, LOW) BUILTIN;

  DCL input_buffer   CHAR(600);
  DCL output_buffer  CHAR(600);
  DCL INFILE         FILE RECORD INPUT;
  DCL OUTFILE        FILE RECORD OUTPUT;
  DCL ERRFILE        FILE RECORD OUTPUT;
  DCL risk_score     FIXED DEC(5,2) INIT(0);
  DCL threshold      FIXED DEC(5,2) INIT(75.00);
  DCL exposure       FIXED DEC(17,2) INIT(0);
  DCL max_exposure   FIXED DEC(17,2) INIT(0);
  DCL probability    FLOAT DEC(16) INIT(0);
  DCL loss_given_def FLOAT DEC(16) INIT(0);
  DCL expected_loss  FIXED DEC(17,2) INIT(0);
  DCL risk_factor_01 FLOAT DEC(16);
  DCL risk_factor_02 FLOAT DEC(16);
  DCL risk_factor_03 FLOAT DEC(16);
  DCL risk_factor_04 FLOAT DEC(16);
  DCL risk_factor_05 FLOAT DEC(16);
  DCL batch_var_0018  FIXED DEC(15,2) INIT(0);
  DCL batch_str_0018  CHAR(40) INIT('');
  DCL batch_var_0019  FIXED DEC(15,2) INIT(0);
  DCL batch_str_0019  CHAR(40) INIT('');
  DCL batch_var_0020  FIXED DEC(15,2) INIT(0);
  DCL batch_str_0020  CHAR(40) INIT('');
  DCL batch_var_0021  FIXED DEC(15,2) INIT(0);
  DCL batch_str_0021  CHAR(40) INIT('');
  DCL batch_var_0022  FIXED DEC(15,2) INIT(0);
  DCL batch_str_0022  CHAR(40) INIT('');
  DCL batch_var_0023  FIXED DEC(15,2) INIT(0);
  DCL batch_str_0023  CHAR(40) INIT('');

  /* Main processing logic */
  CALL init_processing;
  DO WHILE (^eof_flag);
    CALL process_record;
  END;
  CALL finalize_processing;

  /*---------------------------------------------------------------*/
  /* process_record                                                    */
  /*---------------------------------------------------------------*/
  process_record: PROCEDURE;
    records_read = records_read + 1;
    CALL parse_model_data;
    CALL calculate_model_score;
    IF risk_score > threshold THEN
      CALL flag_high_risk;
    CALL write_result;
    records_ok = records_ok + 1;
    READ FILE(INFILE) INTO(input_buffer);
  END process_record;

  /*---------------------------------------------------------------*/
  /* parse_model_data                                                    */
  /*---------------------------------------------------------------*/
  parse_model_data: PROCEDURE;
    /* Parse input record into risk fields */
    exposure = 0;
    probability = 0;
    loss_given_def = 0;
  END parse_model_data;

  /*---------------------------------------------------------------*/
  /* calculate_model_score                                                    */
  /*---------------------------------------------------------------*/
  calculate_model_score: PROCEDURE;
    /* MODEL risk scoring model */
    expected_loss = exposure * probability * loss_given_def;
    risk_score = (expected_loss / exposure) * 100;
    IF risk_score > 100 THEN risk_score = 100;
    IF risk_score < 0 THEN risk_score = 0;
  END calculate_model_score;

  /*---------------------------------------------------------------*/
  /* flag_high_risk                                                    */
  /*---------------------------------------------------------------*/
  flag_high_risk: PROCEDURE;
    PUT SKIP LIST('HIGH RISK: score=' || risk_score
      || ' exposure=' || exposure);
  END flag_high_risk;

  /*---------------------------------------------------------------*/
  /* write_result                                                    */
  /*---------------------------------------------------------------*/
  write_result: PROCEDURE;
    output_buffer = input_buffer;
    WRITE FILE(OUTFILE) FROM(output_buffer);
  END write_result;

  /*---------------------------------------------------------------*/
  /* variant_09_step_00                                                    */
  /*---------------------------------------------------------------*/
  variant_09_step_00: PROCEDURE;
    /* Variant 9 processing step 0 */
    batch_var_0018 = batch_var_0018 + 1;
    IF batch_var_0018 > 1000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 2;
    IF batch_var_0018 > 2000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 3;
    IF batch_var_0018 > 3000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 4;
    IF batch_var_0018 > 4000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 5;
    IF batch_var_0018 > 5000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 6;
    IF batch_var_0018 > 6000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 7;
    IF batch_var_0018 > 7000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 8;
    IF batch_var_0018 > 8000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 9;
    IF batch_var_0018 > 9000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 10;
    IF batch_var_0018 > 10000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 11;
    IF batch_var_0018 > 11000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 12;
    IF batch_var_0018 > 12000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 13;
    IF batch_var_0018 > 13000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 14;
    IF batch_var_0018 > 14000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 15;
    IF batch_var_0018 > 15000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 16;
    IF batch_var_0018 > 16000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 17;
    IF batch_var_0018 > 17000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 18;
    IF batch_var_0018 > 18000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 19;
    IF batch_var_0018 > 19000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 20;
    IF batch_var_0018 > 20000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 21;
    IF batch_var_0018 > 21000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 22;
    IF batch_var_0018 > 22000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 23;
    IF batch_var_0018 > 23000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 24;
    IF batch_var_0018 > 24000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
    batch_var_0018 = batch_var_0018 + 25;
    IF batch_var_0018 > 25000 THEN DO;
      PUT SKIP LIST('Threshold reached at ' || batch_var_0018);
    END;
  END variant_09_step_00;

  init_processing: PROCEDURE;
    PUT SKIP LIST(program_name || ' started at ' || DATETIME());
    OPEN FILE(INFILE) INPUT;
    OPEN FILE(OUTFILE) OUTPUT;
    OPEN FILE(ERRFILE) OUTPUT;
    ON ENDFILE(INFILE) eof_flag = '1'B;
    READ FILE(INFILE) INTO(input_buffer);
  END init_processing;

  finalize_processing: PROCEDURE;
    CLOSE FILE(INFILE);
    CLOSE FILE(OUTFILE);
    CLOSE FILE(ERRFILE);
    PUT SKIP LIST(program_name || ' complete.');
    PUT SKIP LIST('Records read:    ' || records_read);
    PUT SKIP LIST('Records OK:      ' || records_ok);
    PUT SKIP LIST('Records error:   ' || records_err);
    IF records_err > 0 THEN return_code = 4;
  END finalize_processing;

END PLRMODE09V09;
